hubot docker

# Configuration

1. cp docker-compose.yml.sample docker-compose.yml
1. docker-compose.yml
    1. hubot setting
        ```
          OWNER: ${USERNAME}
          BOTNAME: <botname>
          DESC: <description>
          ADAPTER: <adapter>
        ```
    1. set environment
