hubot docker

# Configuration

1. docker-compose.yml
    ```
      OWNER: ${USERNAME}
      BOTNAME: <botname>
      DESC: <description>
      ADAPTER: <adapter>
    ```
1. config.sh
    1. copy config.sh.sample -> config.sh
    1. set environments
